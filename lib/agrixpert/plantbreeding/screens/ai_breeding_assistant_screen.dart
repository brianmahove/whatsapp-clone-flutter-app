import 'package:flutter/material.dart';

class AiBreedingAssistantScreen extends StatelessWidget {
  const AiBreedingAssistantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AI Breeding Assistant')),
      body: const Center(
        child: Text(
          'Chat-style AI breeding assistant will be implemented here.',
        ),
      ),
    );
  }
}
